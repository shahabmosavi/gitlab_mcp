# Personal access token from GitLab (Settings -> Access Tokens).
# Needs the "api" scope for full read/write access.
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# Only needed for self-hosted GitLab. Defaults to https://gitlab.com.
# GITLAB_URL=https://gitlab.example.com
